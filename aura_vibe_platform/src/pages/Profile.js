/**
 * Profile: User's profile view/edit.
 * TODO: Connect to backend, show profile details, avatar, bio, user's posts and allow editing.
 */

import React from "react";
export default function Profile() {
  return (
    <section>
      <h2 className="text-2xl font-bold mb-4">Profile</h2>
      {/* TODO: Profile Card, Edit Button */}
      <div className="rounded-2xl p-8 mb-4 bg-[#18181b] flex flex-col gap-3 opacity-80 italic">
        Profile info and posts…
      </div>
    </section>
  );
}
